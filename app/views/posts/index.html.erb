<h1>TOP</h1>
<% @posts.each do |a| %>
  <% if b = User.find_by(id: a.user_id) %>
  <a>「<%= b.name %>」<% else %><%= "ゲスト" %><% end %> さんの投稿</a><br>
  ・<%= a.content %></br>
  <%= link_to "詳細", "/posts/#{a.id}" %></br>
  </br>
<% end %>